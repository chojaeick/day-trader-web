from pathlib import Path
import sys
root=Path(sys.argv[1] if len(sys.argv)>1 else '.')
engine_path=root/'live_server'/'v4_engine.py'
api_path=root/'live_server'/'api.py'
app_path=root/'app.py'
engine=engine_path.read_text(encoding='utf-8')
api=api_path.read_text(encoding='utf-8')
app=app_path.read_text(encoding='utf-8')

if 'shadow_allow_unknown_quality=False' in engine:
    raise SystemExit('PATCH ABORTED: V4.6.2 engine already present; no files changed.')
if '/api/v4/discovery-bridge-shadow' in api:
    raise SystemExit('PATCH ABORTED: V4.6.2 API already present; no files changed.')
if 'Discovery Bridge Shadow Audit' in app:
    raise SystemExit('PATCH ABORTED: V4.6.2 UI already present; no files changed.')

repls_engine=[
('    def build_usa_finder(self,candidates,discovery,limit=5,db=None):\n','    def build_usa_finder(self,candidates,discovery,limit=5,db=None,commit=True,shadow_allow_unknown_quality=False):\n'),
("        for c in candidates or []:\n            sym=str(c.get('symbol') or '').upper(); q=qmap.get(sym) or {}; quality=q.get('quality_grade')\n            if quality not in ('A','B_EVENT','C_HIGH_RISK'):continue\n            price=_f(c.get('price')); dv=_f(c.get('dollar_volume')); rvol=_f(c.get('rvol')); atr=abs(_f(c.get('atr_pct'))); chg=_f(c.get('change_pct'))\n","        for c in candidates or []:\n            sym=str(c.get('symbol') or '').upper(); q=qmap.get(sym) or {}; quality=q.get('quality_grade')\n            shadow_quality_unknown=False\n            if quality not in ('A','B_EVENT','C_HIGH_RISK'):\n                # V4.6.2 Discovery Bridge Shadow:\n                # production still rejects rows without verified discovery quality.\n                # Shadow may evaluate Screener-eligible misses conservatively with\n                # zero quality bonus. This never mutates live Finder when commit=False.\n                if shadow_allow_unknown_quality and bool(c.get('eligible')):\n                    quality='SHADOW_UNKNOWN'\n                    shadow_quality_unknown=True\n                else:\n                    continue\n            price=_f(c.get('price')); dv=_f(c.get('dollar_volume')); rvol=_f(c.get('rvol')); atr=abs(_f(c.get('atr_pct'))); chg=_f(c.get('change_pct'))\n"),
("                'observed_power':observed_power,'fade_penalty':round(fade_penalty,1),\n                'extreme_continue':extreme_continue,\n                'extreme_watch':quality=='C_HIGH_RISK',\n                'source_origin':q.get('origin'),\n                'inverse_candidate':sym in inverse_syms\n            })\n","                'observed_power':observed_power,'fade_penalty':round(fade_penalty,1),\n                'recent_bars':recent.get('bars'),\n                'extreme_continue':extreme_continue,\n                'extreme_watch':quality=='C_HIGH_RISK',\n                'shadow_quality_unknown':shadow_quality_unknown,\n                'source_origin':q.get('origin'),\n                'inverse_candidate':sym in inverse_syms\n            })\n"),
("        selected=selected[:limit]\n        for i,r in enumerate(selected,1):r['rank']=i\n        self._update_finder('USA',selected)\n        self.finder['USA']['light_rows']=light_rows\n        self.finder['USA']['light_count']=len(light_rows)\n        self.finder['USA']['rotation_seconds']=30\n        self.finder['USA']['market_regime']=regime\n        self.finder['USA']['preferred_direction']='INVERSE' if regime in ('BEAR','STRONG_BEAR') else 'LONG'\n        return self.finder['USA']\n","        selected=selected[:limit]\n        for i,r in enumerate(selected,1):r['rank']=i\n\n        result={\n            'rows':selected,\n            'updated_at':_now(),\n            'session':_session('USA'),\n            'light_rows':light_rows,\n            'light_count':len(light_rows),\n            'rotation_seconds':30,\n            'market_regime':regime,\n            'preferred_direction':'INVERSE' if regime in ('BEAR','STRONG_BEAR') else 'LONG',\n            'shadow':not commit,\n        }\n        if not commit:\n            return result\n\n        self._update_finder('USA',selected)\n        self.finder['USA']['light_rows']=light_rows\n        self.finder['USA']['light_count']=len(light_rows)\n        self.finder['USA']['rotation_seconds']=30\n        self.finder['USA']['market_regime']=regime\n        self.finder['USA']['preferred_direction']='INVERSE' if regime in ('BEAR','STRONG_BEAR') else 'LONG'\n        return self.finder['USA']\n"),
]
for old,new in repls_engine:
    if old not in engine:
        raise SystemExit('PATCH ABORTED: engine anchor missing; no files changed.')
    engine=engine.replace(old,new,1)

if "@app.get('/api/v4/validation/marks')\n" not in api:
    raise SystemExit('PATCH ABORTED: API anchor missing; no files changed.')
api=api.replace("@app.get('/api/v4/validation/marks')\n","\n@app.get('/api/v4/discovery-bridge-shadow')\ndef v4_discovery_bridge_shadow(market:str='USA'):\n    market=str(market or 'USA').upper()\n    if market!='USA':\n        return {\n            'market':market,'supported':False,\n            'note':'Discovery Bridge Shadow is USA-first until verified Korea discovery/minute coverage is available.'\n        }\n\n    candidates=screener_rows(db.quotes(),db.daily_metrics(),40)\n    discovery=k.discovery if isinstance(getattr(k,'discovery',None),dict) else {}\n    live=(getattr(v4,'finder',{}) or {}).get('USA') or {}\n\n    # Same Finder formula, no state mutation / no TOP5 events.\n    shadow=v4.build_usa_finder(\n        candidates,discovery,5,db=db,\n        commit=False,shadow_allow_unknown_quality=True\n    )\n\n    live_rows=live.get('rows') or []\n    live_light=live.get('light_rows') or []\n    shadow_rows=shadow.get('rows') or []\n    shadow_light=shadow.get('light_rows') or []\n\n    live_f={str(r.get('symbol') or '').upper():r for r in live_rows}\n    live_l={str(r.get('symbol') or '').upper():r for r in live_light}\n    sh_f={str(r.get('symbol') or '').upper():r for r in shadow_rows}\n    sh_l={str(r.get('symbol') or '').upper():r for r in shadow_light}\n\n    dset={str(r.get('symbol') or '').upper() for r in (discovery.get('rows') or []) if r.get('symbol')}\n    eset={str(r.get('symbol') or '').upper() for r in (discovery.get('extreme_rows') or []) if r.get('symbol')}\n    qrset={str(r.get('symbol') or '').upper() for r in (discovery.get('quality_risk_rows') or []) if r.get('symbol')}\n\n    misses=[]\n    for c in candidates:\n        sym=str(c.get('symbol') or '').upper()\n        if not sym or sym in dset or sym in eset or sym in qrset:\n            continue\n        srow=sh_l.get(sym) or sh_f.get(sym)\n        misses.append({\n            'symbol':sym,\n            'screener_score':c.get('score'),\n            'change_pct':c.get('change_pct'),\n            'eligible':c.get('eligible'),\n            'rvol':c.get('rvol'),\n            'atr_pct':c.get('atr_pct'),\n            'dollar_volume':c.get('dollar_volume'),\n            'shadow_light_rank':(srow or {}).get('light_rank'),\n            'shadow_finder_rank':(sh_f.get(sym) or {}).get('rank'),\n            'shadow_finder_score':(srow or {}).get('finder_score'),\n            'shadow_quality':(srow or {}).get('quality'),\n            'recent_bars':(srow or {}).get('recent_bars'),\n            'fresh':(srow or {}).get('fresh_mode'),\n            'fresh_score':(srow or {}).get('fresh_score'),\n            'ret_1m':(srow or {}).get('ret_1m'),\n            'ret_3m':(srow or {}).get('ret_3m'),\n            'ret_5m':(srow or {}).get('ret_5m'),\n            'ret_15m':(srow or {}).get('ret_15m'),\n            'volume_accel':(srow or {}).get('volume_accel'),\n            'break_3m_high':(srow or {}).get('break_3m_high'),\n            'would_reach_light':sym in sh_l,\n            'would_reach_finder':sym in sh_f,\n            'note':(\n                'Finder Shadow TOP5' if sym in sh_f else\n                'Light Shadow only' if sym in sh_l else\n                'Shadow에서도 Light20 미진입'\n            )\n        })\n\n    misses.sort(\n        key=lambda r:(\n            1 if r.get('would_reach_finder') else 0,\n            1 if r.get('would_reach_light') else 0,\n            float(r.get('shadow_finder_score') or -1),\n            float(r.get('screener_score') or 0)\n        ),\n        reverse=True\n    )\n\n    live_syms=[str(r.get('symbol') or '').upper() for r in live_rows]\n    shadow_syms=[str(r.get('symbol') or '').upper() for r in shadow_rows]\n    new_shadow=[s for s in shadow_syms if s not in live_syms]\n    displaced=[s for s in live_syms if s not in shadow_syms]\n\n    comparison=[]\n    for i in range(max(len(live_rows),len(shadow_rows))):\n        lr=live_rows[i] if i<len(live_rows) else {}\n        sr=shadow_rows[i] if i<len(shadow_rows) else {}\n        comparison.append({\n            'rank':i+1,\n            'live_symbol':lr.get('symbol'),\n            'live_score':lr.get('finder_score'),\n            'shadow_symbol':sr.get('symbol'),\n            'shadow_score':sr.get('finder_score'),\n            'shadow_unknown_quality':sr.get('shadow_quality_unknown'),\n        })\n\n    return {\n        'market':'USA','supported':True,\n        'updated_at':datetime.now(timezone.utc).isoformat(),\n        'live_finder':live_syms,\n        'shadow_finder':shadow_syms,\n        'new_shadow_entrants':new_shadow,\n        'displaced_live':displaced,\n        'comparison':comparison,\n        'miss_rows':misses,\n        'shadow_light_count':len(shadow_light),\n        'unknown_quality_policy':'Screener eligible + missing verified discovery quality => SHADOW_UNKNOWN, quality bonus 0',\n        'note':'Shadow only. Same Finder scoring path, commit=False. No live Finder state/event/order is changed.'\n    }\n\n"+'\n'+"@app.get('/api/v4/validation/marks')\n",1)

if "            st.markdown('##### 🔄 Inverse / Leveraged ETF 파이프라인')\n" not in app:
    raise SystemExit('PATCH ABORTED: app anchor missing; no files changed.')
app=app.replace("            st.markdown('##### 🔄 Inverse / Leveraged ETF 파이프라인')\n","            st.markdown('##### 🌉 Discovery Bridge Shadow Audit')\n            st.caption('Discovery 누락 종목을 실제 Finder에 넣지 않고, 동일 Finder 점수식으로 가상 재평가합니다. 품질정보가 없는 Screener eligible 종목은 SHADOW_UNKNOWN으로 두고 Quality 보너스 0점으로 계산합니다.')\n            bridge=api(f'/api/v4/discovery-bridge-shadow?market={m}') or {}\n            if bridge.get('supported'):\n                b1,b2,b3=st.columns(3)\n                b1.metric('가상 Finder 신규',len(bridge.get('new_shadow_entrants') or []))\n                b2.metric('가상으로 밀려난 기존',len(bridge.get('displaced_live') or []))\n                b3.metric('Shadow Light',bridge.get('shadow_light_count',0))\n\n                comp=pd.DataFrame(bridge.get('comparison') or [])\n                if len(comp):\n                    st.markdown('###### Live Finder vs Bridge Shadow Finder')\n                    st.dataframe(comp,use_container_width=True,hide_index=True)\n\n                miss_shadow=pd.DataFrame(bridge.get('miss_rows') or [])\n                if len(miss_shadow):\n                    st.markdown('###### Discovery Miss를 가상으로 넣었을 때')\n                    cols=[c for c in [\n                        'symbol','screener_score','change_pct','eligible',\n                        'shadow_light_rank','shadow_finder_rank','shadow_finder_score',\n                        'shadow_quality','recent_bars','fresh','fresh_score',\n                        'ret_1m','ret_3m','ret_5m','ret_15m','volume_accel',\n                        'break_3m_high','would_reach_light','would_reach_finder','note'\n                    ] if c in miss_shadow.columns]\n                    st.dataframe(miss_shadow[cols],use_container_width=True,hide_index=True)\n\n                entrants=bridge.get('new_shadow_entrants') or []\n                displaced=bridge.get('displaced_live') or []\n                if entrants:\n                    st.warning('Bridge Shadow에서 Finder TOP5에 새로 들어오는 누락 종목 · '+', '.join(entrants))\n                    if displaced:\n                        st.write('가상으로 밀려나는 기존 Finder · '+', '.join(displaced))\n                else:\n                    st.success('현재 Discovery 누락 종목을 보수적으로 가상 연결해도 Finder TOP5 변화는 없습니다.')\n\n                st.caption('주의: 이 Shadow는 품질정보 누락 종목에 Quality 보너스를 주지 않습니다. minute bars가 부족하면 recent/Fresh가 0에 가깝게 평가될 수 있으므로 recent_bars도 함께 봐야 합니다.')\n            else:\n                st.info(bridge.get('note') or 'Bridge Shadow는 현재 USA만 지원합니다.')\n\n            st.markdown('##### 🔄 Inverse / Leveraged ETF 파이프라인')\n",1)
if "st.caption('V4.6.1 · LIGHT→FINDER CUTLINE + DISCOVERY MISS AUDIT · MAX 5 HEAVY TRACKING · MANUAL ORDER ONLY')" in app:
    app=app.replace("st.caption('V4.6.1 · LIGHT→FINDER CUTLINE + DISCOVERY MISS AUDIT · MAX 5 HEAVY TRACKING · MANUAL ORDER ONLY')","st.caption('V4.6.2 · DISCOVERY BRIDGE SHADOW AUDIT · MAX 5 HEAVY TRACKING · MANUAL ORDER ONLY')",1)

engine_path.write_text(engine,encoding='utf-8')
api_path.write_text(api,encoding='utf-8')
app_path.write_text(app,encoding='utf-8')
print('PATCHED:',engine_path)
print('PATCHED:',api_path)
print('PATCHED:',app_path)
